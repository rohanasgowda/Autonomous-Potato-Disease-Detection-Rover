# Alembic Placeholder

The Phase 2 requirements identify Alembic as preferred tooling, but the prototype
uses SQLAlchemy `create_all` for the initial SQLite schema to keep local setup
small and deterministic.

Add Alembic migration files here when schema evolution begins.
